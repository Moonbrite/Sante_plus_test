{% extends 'base.html.twig' %}

{% block title %}Appointment{% endblock %}

{% block body %}
    <h1>Appointment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Day_of_appointment</th>
                <th>Name Medecin</th>
                <th>Name Patient</th>
            </tr>
            <tr>
                <td>{{ appointment.id }}</td>
                <td>{{ appointment.dayOfAppointment ? appointment.dayOfAppointment|date('Y-m-d') : '' }}</td>
                <td>{{ appointment.getMedecin }}</td>
                <td>{{ appointment.getUser.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_appointment_index') }}">back to list</a>

    <a href="{{ path('app_appointment_edit', {'id': appointment.id}) }}">edit</a>

    {{ include('appointment/_delete_form.html.twig') }}
{% endblock %}
